# config for RdbEX.

# edit with caution, changing things could potentially break existing databases.

sep = '|' # seperator for paths. do not set it to "/" or "\". (ex: a|b|c)

ref = '[//' # reference spacing.

# enter custom banned characters for key and folder names here. 
# "#", all sets of brackets, and the seperator character are banned by default
banned_characters = []

# enter message to append to front of full key names. 
# this will only show if the user is using the DB without RdbEX installed.

msg = "RdbEX"